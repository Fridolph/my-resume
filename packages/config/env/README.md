# packages/config/env

共享环境变量约定占位目录。

## 未来职责

- 统一 `web`、`admin`、`server` 的环境变量命名约定
- 区分客户端可暴露变量与服务端私有变量
- 为后续真实校验方案提供统一入口

## 当前阶段不做

- 不引入 `zod`、`envalid` 等校验库
- 不提供 `.env.example` 全量模板
- 不接入运行时校验逻辑
