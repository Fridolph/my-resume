# packages

当前目录用于承载跨应用共享的包。

当前阶段（`M1 / issue-02`）只补齐目标共享包目录占位，不创建真实实现。

## 目录规划

- `ui`：共享 React UI 组件
- `api-client`：共享 API 调用层与类型
- `config`：共享工程配置
- `utils`：共享纯函数与基础工具

## 当前边界

- 只建立目录与说明文件
- 不编写共享包代码
- 不引入真实业务依赖
