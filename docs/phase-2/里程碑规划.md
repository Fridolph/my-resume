# 第二阶段执行里程碑

## 说明

第二阶段原始的 P1 已经完成了一次“真实数据链路可行性验证”，但它采用的是过渡方案，不再作为后续正式技术主线继续扩展。

从当前开始，第二阶段的执行计划正式切换到三端统一架构：

- `apps/web`：Nuxt 4 展示端
- `apps/admin`：Nuxt 4 管理后台前端
- `apps/api-server`：NestJS API Server
- 数据层：`SQLite + Drizzle ORM`

因此，下面的里程碑以“统一架构落地”为起点重新规划，并直接覆盖旧计划。

## 已完成前置探索：过渡版 P1

### 已完成内容

- 完成 SQLite 落盘与共享数据文件验证
- 完成首个真实样板模块：站点设置
- 完成后台写入与前台读取的链路验证
- 完成阶段性技术博客记录

### 当前定位

> 这部分工作作为第二阶段的技术探索资产保留，但不再继续扩展为正式后端方案。后续实现统一迁移到 `NestJS + Drizzle ORM`。

---

## P1：API Server 与数据库基座重建

### 目标

建立第二阶段正式使用的后端基础设施，让项目拥有统一 API 入口与正式数据层。

### 子任务

- [x] 创建 `apps/api-server` NestJS 项目骨架
- [x] 建立 `HealthModule`
- [x] 建立 `DatabaseModule`
- [x] 将 `packages/database` 重构为 Drizzle ORM 版本
- [x] 建立 Drizzle schema、client 与 migration 基础结构
- [x] 配置 SQLite 数据源与开发环境变量约定
- [x] 完成 API Server 本地启动与健康检查
- [x] 保持现有 Admin / Web 对共享数据库包的兼容

### 完成标准

- NestJS 服务可以本地启动
- `packages/database` 已切换为 Drizzle ORM 正式方案
- 数据库迁移可以执行
- Web / Admin 后续具备接入统一 API Server 的基础

### 完成总结

> 已完成。当前仓库已新增 `apps/api-server`，建立 `HealthModule` 与 `DatabaseModule`，并将 `packages/database` 重构为 `SQLite + Drizzle ORM` 方案。当前运行时使用 `node:sqlite` 作为 SQLite 驱动，以避免 `better-sqlite3` 原生绑定问题，同时保持 ORM 标准层统一为 Drizzle。`pnpm --dir packages/database db:migrate`、`pnpm --dir apps/api-server typecheck`、`pnpm --dir apps/admin typecheck` 与 `pnpm --dir apps/web typecheck` 已通过，本地也已验证 `GET /api/health` 可正常返回。

---

## P2：站点设置模块正式迁移闭环

### 目标

把过渡样板迁移到正式架构，形成第一条三端统一链路。

### 子任务

- [x] 在 `apps/api-server` 建立 `SiteSettingsModule`
- [x] 提供站点设置查询与更新接口
- [x] 将 `packages/sdk` 增加站点设置 API Client
- [x] 将 `apps/admin` 设置页改为调用 API Server
- [x] 将 `apps/web` 改为通过 API Server 读取站点设置
- [x] 移除原有 Nuxt 内部临时 API 路由

### 完成标准

- 站点设置通过 NestJS API 完成读写
- Admin 与 Web 使用统一 API 源
- 过渡版 P1 的站点配置链路完成正式迁移

### 完成总结

> 已完成。当前仓库已在 `apps/api-server` 中建立 `SiteSettingsModule`，并开放 `GET /api/site-settings` 与 `PUT /api/site-settings`。`packages/sdk` 已补充站点设置 API Client，`apps/admin` 设置页与 `apps/web` 站点设置读取已切换到统一 API Server，原有 Nuxt 内部 `site-settings` 临时路由也已移除。本地已验证 API 读写、Admin 页面显示、Web 站点设置读取，以及 SQLite 数据落盘同步成功。

> 附加记录：本阶段还补充了 Drizzle Studio 与 `better-sqlite3` 的排障记录，以及一篇面向新人的 `SQLite + Drizzle ORM` 入门指南，便于后续继续迁移数据库模块时复用。

---

## P3：后台核心内容模块接入

### 目标

让后台核心内容模块进入正式持久化阶段。

### 子任务

- [x] 用户模块表结构与接口
- [x] 文案模块表结构与接口
- [x] 简历模块表结构与接口
- [x] 项目模块表结构与接口
- [x] 表单校验、错误处理与统一响应结构
- [x] Admin 页面逐步切换为 API 驱动

### 完成标准

- Admin 核心模块不再依赖本地状态或 mock 数据
- 核心内容具备真实增删改查能力
- 数据模型边界与 DTO 边界清晰

### 完成总结

> 已完成。当前 Web 首页已经开始聚合 `site-settings` 与 `projects` 的真实 API 数据，简历页、项目列表页与项目详情页也都已切换到 `api-server`。同时，导航、Footer、首页说明等公开文案已经支持后台翻译结果覆盖，并采用“后台翻译优先、前端静态字典兜底”的方式稳定运行。至此，`web` 端的核心公开内容已经正式接入统一 API Server。

---

## P4：Web 公开内容全面接入后台数据

### 目标

让 Web 从“结构展示”升级为“消费后台真实内容”的正式前台。

### 子任务

- [x] 首页接入真实站点设置与公开文案
- [x] 简历页接入真实简历数据
- [x] 项目列表与详情页接入真实项目数据
- [x] 多语言公开文案接入后台管理结果
- [x] `packages/sdk` 建立公开内容查询能力

### 完成标准

- Web 主要内容全部来自正式 API Server
- mock 数据逐步退出主链路
- 前后台真正完成内容打通

### 完成总结

> 已完成。当前 `apps/web` 已完成首页、简历页、项目列表页和项目详情页的真实 API 接入；其中首页会聚合 `site-settings` 与 `projects` 数据生成展示内容，导航、Footer、首页说明等公开文案已支持后台翻译覆盖。`packages/sdk` 也已经具备站点设置、项目、简历与翻译查询能力，Web 主链路的公开内容正式完成 API 化。

---

## P5：用户、权限与管理边界完善

### 目标

让后台从“内容管理页集合”升级为真正可控的管理系统。

### 子任务

- [x] P5-1：定义统一角色集合与权限清单
- [x] P5-1：收口共享角色权限映射
- [x] P5-2：建立基础鉴权机制
- [x] P5-3：Admin 接入真实登录态
- [x] P5-4：建立权限守卫与接口级访问控制
- [x] P5-5：Admin 页面接入真实权限判断
- [x] P5-6：梳理公开接口与后台接口边界

### 完成标准

- API Server 具备基础认证和权限能力
- Admin 的页面访问与接口访问具备边界控制
- 用户管理开始脱离静态演示状态

### 当前进展

> `P5-1`、`P5-2`、`P5-3`、`P5-4`、`P5-5` 与 `P5-6` 已完成。当前项目已经完成公开站点接口与后台管理接口的路径分区，后台读取接口也已纳入权限控制。下一步进入 `P6`，开始内容发布流与版本能力建设。

---

## P6：内容发布流与版本能力

### 目标

为文案、简历、项目等模块建立更完整的内容工作流。

### 子任务

- [x] P6-1：建立统一内容状态流转规则
- [x] P6-2：增加发布时间、更新人、审核人字段
- [x] P6-3：建立版本记录或历史快照能力
- [x] P6-4：建立回滚或恢复策略
- [x] P6-5：梳理多模块统一发布策略

### 完成标准

- 关键内容具备发布与追踪能力
- 管理后台开始拥有真正的工作流能力
- 数据模型支持后续审计与协作扩展

### 当前进展

> 当前 `P6-1`、`P6-2`、`P6-3`、`P6-4` 与 `P6-5` 已完成。项目已经建立模块级工作流与站点级统一发布批次能力。下一步进入 `P7`，开始测试、部署与环境收口。

---

## P7：测试、部署与环境收口

### 目标

为第二阶段收尾建立质量保障、运行规范与部署基础，让项目进入可验证、可维护、可交付阶段。

### 子任务

- [x] P7-1：API Server 关键链路测试补齐
- [x] P7-2：Web / Admin 关键流程验证收口
- [x] P7-3：环境变量与多端启动文档收口
- [ ] P7-4：部署方案与上线前检查项整理
- [ ] P7-5：第二阶段总结文档与交付清单整理

### 完成标准

- API Server 关键工作流具备自动化测试基础
- Web / Admin 关键流程具备可重复验证清单
- 三端环境变量、启动方式与常用命令文档清晰
- 项目具备基础部署蓝图与上线前检查项
- 第二阶段形成完整总结与交付材料

### 当前计划

> `P7-1`、`P7-2` 与 `P7-3` 已完成。当前仓库已经补齐三端环境变量说明、统一的 `.env.example`、根目录与应用级 README，以及根目录数据库命令入口。下一步进入 `P7-4`，整理部署方案与上线前检查项。

## 当前建议执行顺序

当前建议继续按第二阶段后半程节奏推进：

1. 先完成 `P5` 的认证、权限与接口边界
2. 再进入 `P6` 的发布流、版本与工作流能力
3. 最后收口 `P7` 的测试、部署与环境整理

## 当前里程碑状态

- 过渡版 P1：已完成，保留为探索记录
- 正式版 P1：已完成
- P2：已完成
- P3：已完成
- P4：已完成
- P5：已完成
- P6：已完成
- 当前下一步：开始 `P7-4`，部署方案与上线前检查项整理
