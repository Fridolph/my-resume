# 第二阶段执行里程碑

## P1：真实数据层与 API 基础设施

### 子任务

- [x] 选择数据库方案：SQLite
- [x] 选择数据访问层：Node.js 内建 SQLite + Repository
- [x] 建立共享数据库包 `packages/database`
- [x] 建立首个真实 API 路由样板
- [x] 建立基础环境与本地数据库文件约定
- [x] 选定首个样板模块：站点配置
- [ ] 将更多后台模块接入真实持久化（后续 P2）
- [ ] 将前台读取切换为真实服务（后续 P3）

### 完成总结

> 已完成。P1 已确定数据库方案为 SQLite，数据访问层为 Node.js 内建 `node:sqlite` + Repository，并建立共享数据库包、后台站点配置真实 API、Web 只读站点配置 API 与首个真实数据持久化样板。`pnpm --dir apps/admin typecheck` 与 `pnpm --dir apps/web typecheck` 已通过，本地也已验证 admin 写入、web 读取和 SQLite 文件落盘。
