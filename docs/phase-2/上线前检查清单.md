# 上线前检查清单

## 一、环境变量

- [ ] `api-server` 已配置 `PORT`
- [ ] `api-server` 已配置 `REPO_DATABASE_PATH`
- [ ] `api-server` 已按需配置 `CORS_ORIGIN`
- [ ] `web` 已配置 `NUXT_PUBLIC_PUBLIC_API_BASE_URL`
- [ ] `web` 已配置 `NUXT_PUBLIC_SITE_URL`
- [ ] `admin` 已配置 `NUXT_PUBLIC_ADMIN_API_BASE_URL`

## 二、数据库

- [ ] 已备份现有 SQLite 数据文件
- [ ] `REPO_DATABASE_PATH` 指向持久化目录
- [ ] 已执行 migration
- [ ] `pnpm db:status` 返回正常
- [ ] 当前存在可用的激活发布批次

## 三、API Server

- [ ] `GET /api/health` 返回 `200`
- [ ] `GET /api/public/release` 返回 `200`
- [ ] 登录接口可正常建立会话
- [ ] 管理接口在未登录状态下会正确拒绝
- [ ] 线上 CORS 来源符合预期

## 四、Web

- [ ] 首页可访问
- [ ] 简历页可访问
- [ ] 项目列表页可访问
- [ ] 项目详情页可访问
- [ ] `robots.txt` 可访问
- [ ] `sitemap.xml` 可访问
- [ ] 页面 canonical 指向线上域名
- [ ] OG 图地址可访问

## 五、Admin

- [ ] 登录页可访问
- [ ] 登录后首页可访问
- [ ] 权限受限页面跳转正确
- [ ] 文案、简历、项目、发布中心页面可访问
- [ ] `admin` 请求地址指向正式 `/api/admin`

## 六、发布与回滚

- [ ] 当前激活批次已核对
- [ ] 需要发布的新批次已在后台确认
- [ ] 数据库备份路径已记录
- [ ] 回滚步骤已明确
