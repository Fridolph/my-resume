# M11 / issue-114 M9 主题边界到共享 UI 教程正文

- Issue：`#114`
- 里程碑：`M11 教程正文整理：从大纲到系列文章`
- 分支：`fys-dev/docs-m11-issue-114-m9-ui-article`
- 日期：`2026-03-27`

## 背景

`M2 ~ M8` 正文已经补齐，`M9` 目前仍主要停留在阶段大纲和 issue 级开发日志层。

为了让读者真正理解项目为什么在功能闭环之后，没有立刻进入多模板或完整设计系统，而是先收主题基线、后台壳、公开页视觉节奏与最小共享展示原语，需要把 `M9` 扩写成一篇可连续阅读的正文文章。

## 本次目标

- 在 `docs/50-教程文章/` 中新增 `M9` 正文
- 更新正文目录，让 `M2 -> M9` 阅读路径连续
- 保持正文聚焦 `M9` 的体验基线与共享展示边界，不越界扩写 `M10`
- 继续维持正文、大纲、开发日志三层职责分离

## 非目标

- 不新增功能代码
- 不修改现有 `M9` 实现
- 不同时扩写 `M10`
- 不把文章写成开发日志或提交记录

## Plan

1. 复盘 `M9` 大纲与 `issue-42 ~ issue-46` 开发日志
2. 提炼正文主线，围绕“为什么先收主题边界与共享 UI 基线”组织全文
3. 更新正文目录，补齐本轮开发日志
4. 做文档级 review 与人工校对，再进入提交阶段

## 实际改动

- 更新 `docs/50-教程文章/README.md`
- 新增 `docs/50-教程文章/08-M9-主题边界到共享-UI-从可用到可继续设计.md`
- 新增本篇开发日志，记录本轮文档任务

## Review 记录

### 是否符合当前 Issue 与里程碑目标

符合。当前只做了：

- 增加 `M9` 正文文章
- 更新正文目录入口
- 补充本轮文档开发日志

没有提前进入：

- `M10` 正文扩写
- 功能实现或重构
- 文档体系大规模改动

### 是否存在可继续复用的结构

- 当前正文结构仍可继续复用到后续里程碑：
  - 为什么这个里程碑值得单独成立
  - 本轮真正交付什么
  - issue 拆解顺序
  - 关键设计取舍
  - 关键文件阅读路径
  - 测试重点
  - 容易踩的坑
  - 与下一里程碑的衔接
- `docs/50-教程文章/README.md` 已形成连续正文目录，后续可继续顺序扩展

### Review 结论

- 通过
- 可进入文档级自测

## 自测结果

### 1. 正文目录校对

- 检查 `docs/50-教程文章/README.md` 是否包含 `M2 -> M9` 连续入口
- 结果：通过

### 2. 正文范围校对

- 检查文章是否仍聚焦 `M9` 的主题、后台壳、公开页视觉基线与共享展示原语
- 结果：通过
- 备注：正文围绕体验基线、共享层时机和页面结构组织展开，没有扩写到 `M10` 的 AI 工作台闭环

### 3. 文档分层校对

- 检查正文、大纲、开发日志之间是否职责清晰
- 结果：通过
- 结论：
  - `docs/40-教程与博客/` 继续保留里程碑大纲
  - `docs/30-开发日志/` 继续保留 issue 过程记录
  - `docs/50-教程文章/` 继续沉淀对外正文

## 遇到的问题

### 1. `M9` 很容易被写成“页面变得更好看了”

- 风险：如果只按视觉变化罗列，读者看不出这一轮真正收住的是体验边界和共享时机
- 处理：改为围绕“为什么先收主题、页面壳和共享展示原语”组织全文，把页面升级放回体验基线语境里

### 2. 共享 UI 很容易被误写成“组件库建设”

- 风险：会削弱当前阶段“只抽展示原语、不抽业务语义”的关键判断
- 处理：在正文里明确区分展示原语和业务组件，强调当前共享层只解决重复展示骨架

## 后续可继续沉淀的点

- 继续按相同模板扩写 `M10` 正文
- 当正文数量足够后，再整理成面向外部读者的系列导航或 VitePress 侧边栏
