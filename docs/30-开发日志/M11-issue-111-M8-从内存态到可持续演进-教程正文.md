# M11 / issue-111 M8 从内存态到可持续演进教程正文

- Issue：`#111`
- 里程碑：`M11 教程正文整理：从大纲到系列文章`
- 分支：`fys-dev/docs-m11-issue-111-m8-persistence-article`
- 日期：`2026-03-27`

## 背景

`M2 ~ M7` 正文已经落地，但 `M8` 目前仍主要停留在阶段大纲和 issue 级开发日志层。

为了让读者真正理解“项目为什么要从内存态 demo 继续走向数据库持久化、共享契约与真实联调收束，以及这一轮如何把仓库推进到更可持续演进的状态”，本轮继续按教程整理流程，把 `M8` 扩写为一篇可独立阅读的正文文章。

## 本次目标

- 在 `docs/50-教程文章/` 中新增 `M8` 正文
- 更新正文目录，让 `M2 -> M8` 阅读路径连续
- 保持正文聚焦 `M8` 的数据与契约边界收束，不越界扩写 `M9 ~ M10`
- 继续维持正文、大纲、开发日志三层职责分离

## 非目标

- 不新增功能代码
- 不修改现有 `M8` 实现
- 不同时扩写其他里程碑正文
- 不把文章写成开发日志或提交记录

## Plan

1. 先复盘 `M8` 大纲与 `issue-36 ~ issue-41` 开发日志
2. 提炼正文主线，围绕“为什么先收数据与契约边界”组织全文
3. 更新正文目录，补齐本轮开发日志
4. 做文档级 review 与人工校对，再进入提交阶段

## 实际改动

- 更新 `docs/50-教程文章/README.md`
- 新增 `docs/50-教程文章/07-M8-从内存态到可持续演进-为什么先收数据与契约边界.md`
- 新增本篇开发日志，记录本轮文档任务

## Review 记录

### 是否符合当前 Issue 与里程碑目标

符合。当前只做了：

- 增加 `M8` 正文文章
- 更新正文目录入口
- 补充本轮文档开发日志

没有提前进入：

- `M9 ~ M10` 正文扩写
- 功能实现或重构
- 文档体系大规模改动

### 是否存在可继续复用的结构

- 当前正文结构仍可继续复用到后续里程碑：
  - 为什么这个里程碑值得单独成立
  - 本轮真正交付什么
  - issue 拆解顺序
  - 关键设计取舍
  - 测试重点
  - 容易踩的坑
  - 与下一里程碑的衔接
- `docs/50-教程文章/README.md` 已形成连续正文目录，后续可继续顺序扩展

### Review 结论

- 通过
- 可进入文档级自测

## 自测结果

### 1. 正文目录校对

- 检查 `docs/50-教程文章/README.md` 是否包含 `M2 -> M8` 连续入口
- 结果：通过

### 2. 正文范围校对

- 检查文章是否仍聚焦 `M8` 的数据与契约边界收束
- 结果：通过
- 备注：正文围绕数据库骨架、草稿/发布持久化、共享契约、真实联调与测试主线统一展开，没有扩写到主题升级或 AI 工作台阶段

### 3. 文档分层校对

- 检查正文、大纲、开发日志之间是否职责清晰
- 结果：通过
- 结论：
  - `docs/40-教程与博客/` 继续保留里程碑大纲
  - `docs/30-开发日志/` 继续保留 issue 过程记录
  - `docs/50-教程文章/` 继续沉淀对外正文

## 遇到的问题

### 1. `M8` 容易被写成“数据库接入清单”

- 风险：如果只按工具和文件罗列，读者会看不出这一轮真正收住的是哪些演进边界
- 处理：改为围绕“为什么先收数据与契约边界”组织全文，把持久化、共享契约、真实联调和测试主线统一收成一条基础设施主线

### 2. `issue-41` 容易被误判为与 M8 主线无关

- 风险：会把测试框架统一看成额外杂项，削弱这一轮“可持续演进”主题
- 处理：明确把 Vitest 统一也放回“基础设施收束”语境里，说明它和持久化、共享契约一样，都在帮助仓库走向更稳定的长期演进状态

## 后续可继续沉淀的点

- 继续按相同模板扩写 `M9 ~ M10` 正文
- 当正文数量足够后，再整理成面向外部读者的系列导航或 VitePress 侧边栏
