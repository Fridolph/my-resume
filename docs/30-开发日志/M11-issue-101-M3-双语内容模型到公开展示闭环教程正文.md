# M11 / issue-101 M3 双语内容模型到公开展示闭环教程正文

- Issue：`#101`
- 里程碑：`M11 教程正文整理：从大纲到系列文章`
- 分支：`fys-dev/docs-m11-issue-101-m3-content-article`
- 日期：`2026-03-27`

## 背景

`M2` 正文已经落地，但 `M3` 目前仍主要依赖里程碑大纲和 issue 级开发日志承载上下文。

为了让读者真正理解“标准双语简历是如何从领域模型一路走到草稿 / 发布态，再走到公开展示端”的过程，本轮继续沿教程整理流程，把 `M3` 扩写为一篇可独立阅读的正文文章。

## 本次目标

- 在 `docs/50-教程文章/` 中新增 `M3` 正文
- 更新正文目录，让 `M2 -> M3` 阅读路径连续
- 保持正文聚焦 `M3` 内容闭环，不越界扩写 `M4 ~ M10`
- 让正文、大纲、开发日志三层职责继续清晰分离

## 非目标

- 不新增功能代码
- 不修改 `M3` 既有实现
- 不重写 `M3` 里程碑大纲
- 不同时扩写其他里程碑正文

## Plan

1. 先复盘 `M3` 大纲与 `issue-10 / issue-11 / issue-12 / issue-23` 开发日志
2. 提炼正文主线，围绕“为什么先收内容闭环”组织文章
3. 更新正文目录，补齐本轮开发日志
4. 做文档级 review 与人工校对，再进入提交阶段

## 实际改动

- 更新 `docs/50-教程文章/README.md`
- 新增 `docs/50-教程文章/02-M3-双语内容模型到公开展示-为什么先收内容闭环.md`
- 新增本篇开发日志，记录本轮文档任务

## Review 记录

### 是否符合当前 Issue 与里程碑目标

符合。当前只做了：

- 增加 `M3` 正文文章
- 更新正文目录入口
- 补充本轮文档开发日志

没有提前进入：

- `M4 ~ M10` 正文扩写
- 功能实现或重构
- 文档体系大规模调整

### 是否存在可继续复用的结构

- 当前正文结构可继续复用到后续里程碑：
  - 为什么这个里程碑重要
  - 本轮真正交付什么
  - issue 拆解顺序
  - 关键设计取舍
  - 测试关注点
  - 容易踩的坑
  - 与下一里程碑的衔接
- `docs/50-教程文章/README.md` 已形成连续正文目录，后续可继续顺序扩展

### Review 结论

- 通过
- 可进入文档级自测

## 自测结果

### 1. 正文目录校对

- 检查 `docs/50-教程文章/README.md` 是否包含 `M2 -> M3` 连续入口
- 结果：通过

### 2. 正文范围校对

- 检查文章是否仍聚焦 `M3` 内容闭环
- 结果：通过
- 备注：正文围绕内容模型、草稿 / 发布态、公开展示与主题边界展开，没有扩写到后续 AI 或导出实现

### 3. 文档分层校对

- 检查正文、大纲、开发日志之间是否职责清晰
- 结果：通过
- 结论：
  - `docs/40-教程与博客/` 继续保留里程碑大纲
  - `docs/30-开发日志/` 继续保留 issue 过程记录
  - `docs/50-教程文章/` 继续沉淀对外正文

## 遇到的问题

### 1. `M3` 的实现点比 `M2` 更分散

- 风险：容易把正文写成“按文件或按提交罗列改动”
- 处理：改为围绕“为什么先收内容闭环”组织全文，把四个 issue 收成一条主线

### 2. 大纲与真实日志文件名存在细小差异

- 风险：如果直接照大纲引用，容易出现文档引用不准
- 处理：先用真实开发日志文件名回校，再组织正文引用

## 后续可继续沉淀的点

- 继续按相同模板扩写 `M4 ~ M10` 正文
- 当正文数量足够后，再整理成面向外部读者的系列导航或 VitePress 侧边栏
