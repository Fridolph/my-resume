# apps

当前目录用于承载前端应用与业务后端应用。

当前阶段（`M1 / issue-02`）只补齐目标目录占位，不创建真实框架应用。

## 目录规划

- `web`：公开简历展示站，后续计划使用 `Next.js`
- `admin`：后台管理端，后续计划使用 `Next.js`
- `server`：唯一业务后端，后续计划使用 `NestJS`

## 当前边界

- 只建立目录与说明文件
- 不运行 `create-next-app`
- 不运行 `nest new`
- 不迁移现有根级 Vue 项目
